# Crawl4AI 中文示例 - 快速开始指南

欢迎使用 Crawl4AI 中文示例代码！本指南将帮助你快速上手这个强大的网络爬虫工具。

## 🚀 安装和配置

### 1. 安装 Crawl4AI
```bash
pip install crawl4ai
```

### 2. 安装浏览器依赖
```bash
# 安装 Playwright 浏览器
crawl4ai-setup
# 或者
playwright install
```

### 3. 安装示例依赖
```bash
pip install -r requirements.txt
```

## 📚 示例代码结构

```
examples_chinese/
├── README.md                    # 本文件
├── 快速开始指南.md              # 快速入门指导
├── 进阶示例.md                  # 高级功能教程
├── requirements.txt             # 依赖包列表
├── hello_world.py             # 最基础的示例
├── quickstart.py              # 快速入门示例
├── browser_optimization_example.py  # 浏览器优化
├── hooks_example.py            # 钩子函数使用
└── adaptive_crawling/
    ├── README.md               # 自适应爬虫说明
    └── basic_usage.py          # 基础自适应爬虫
```

## 🎯 学习路径

### 第1步：运行 Hello World
```bash
python hello_world.py
```
这是最简单的示例，展示如何爬取一个网页并提取内容。

### 第2步：快速入门
```bash
python quickstart.py
```
这个示例展示了 Crawl4AI 的主要功能，包括：
- ✅ 基础爬取
- ✅ 内容清理
- ✅ 链接分析
- ✅ JavaScript 执行
- ✅ CSS 选择器
- ✅ 媒体处理
- ✅ 自定义钩子

### 第3步：学习高级功能

#### 浏览器优化
```bash
python browser_optimization_example.py
```
学习如何：
- 优化浏览器性能
- 顺序爬取 vs 并行爬取
- 性能监控

#### 钩子函数
```bash
python hooks_example.py
```
学习如何：
- 在爬虫不同阶段插入自定义代码
- 设置认证信息
- 修改请求头
- 等待特定元素加载

#### 自适应爬虫
```bash
python adaptive_crawling/basic_usage.py
```
学习如何：
- 智能分析内容相关性
- 自动决定停止时机
- 建立知识库

## 🔧 基础概念

### 核心组件

1. **AsyncWebCrawler**: 主要的爬虫类
2. **BrowserConfig**: 浏览器配置
3. **CrawlerRunConfig**: 爬虫运行配置
4. **CacheMode**: 缓存模式

### 基本使用模式

```python
import asyncio
from crawl4ai import AsyncWebCrawler, BrowserConfig, CrawlerRunConfig

async def main():
    # 配置浏览器
    browser_config = BrowserConfig(headless=True)
    
    # 配置爬虫
    crawler_config = CrawlerRunConfig()
    
    # 创建爬虫实例
    async with AsyncWebCrawler(config=browser_config) as crawler:
        # 执行爬取
        result = await crawler.arun(
            url="https://example.com",
            config=crawler_config
        )
        
        # 处理结果
        if result.success:
            print(result.markdown)

if __name__ == "__main__":
    asyncio.run(main())
```

## 🛠️ 常见问题解决

### 1. 浏览器启动失败
```bash
# 确保已安装浏览器依赖
playwright install chromium
```

### 2. 页面加载超时
```python
# 增加超时时间
crawler_config = CrawlerRunConfig(
    timeout=60  # 60秒超时
)
```

### 3. 内存使用过高
```python
# 使用内容过滤
from crawl4ai.content_filter_strategy import PruningContentFilter

crawler_config = CrawlerRunConfig(
    markdown_generator=DefaultMarkdownGenerator(
        content_filter=PruningContentFilter(threshold=0.48)
    )
)
```

### 4. 被反爬虫拦截
```python
# 使用代理
browser_config = BrowserConfig(
    proxy_config={
        "server": "http://proxy.example.com:8080",
        "username": "username",
        "password": "password"
    }
)
```

## 📖 下一步学习

1. **阅读进阶示例**: 查看 `进阶示例.md` 了解更多高级功能
2. **探索原始示例**: 参考 `../examples/` 目录中的更多示例
3. **查看官方文档**: 访问 https://crawl4ai.com 获取最新文档
4. **加入社区**: 在 GitHub 上参与讨论和贡献

## 💡 使用建议

1. **从简单开始**: 先运行基础示例，逐步增加复杂度
2. **关注性能**: 合理设置并发数和超时时间
3. **遵守规则**: 尊重目标网站的 robots.txt 和使用条款
4. **错误处理**: 总是添加适当的错误处理逻辑
5. **资源管理**: 使用上下文管理器确保资源正确释放

## 🆘 获取帮助

- 📚 **文档**: 查看 `进阶示例.md` 获取详细教程
- 🔍 **搜索**: 在 GitHub Issues 中搜索类似问题
- 💬 **提问**: 在 GitHub Discussions 中提问
- 🐛 **报告**: 发现 Bug 请在 GitHub Issues 中报告

祝你使用 Crawl4AI 愉快！🎉