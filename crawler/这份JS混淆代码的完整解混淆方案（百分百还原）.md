# 这份JS混淆代码的完整解混淆方案（百分百还原）

你贴的这种代码是 **JavaScript最经典的「数组常量池+数字下标混淆」**（也叫字符串字典混淆/乱序数组混淆），是前端最常见的混淆手段，特征非常典型：顶部一个超长的字符串数组`_0x495`，业务逻辑中所有字符串/常量都被替换成`_0x495[数字下标]`，配合自执行函数+数组乱序函数做二次加密，**这种混淆的破解难度极低，有「一键自动化还原」和「手动精准还原」两种方案**，你的代码结构完整，还原后能得到100%可读的明文源码。

## ✅ 先看懂你这份混淆的核心原理

你的代码里的核心混淆逻辑拆解，理解后就知道为什么能轻松破解：

1. **核心字典池**：`var _0x495 = ["161", "162", "anya error", "window"...]` 这一行是「所有明文的仓库」，项目里的**字符串、常量、方法名、URL、正则、报错信息、业务密钥**等，全部存在这个数组里；

2. **下标替换**：原代码中所有本应写明文的地方，都被替换成 `_0x495[数字索引]`，比如 `_0x495[15]` = `"Anya"`、`_0x495[16]` = `"window"`、`_0x495[24]` = `"congratulations"`；

3. **数组乱序**：代码里的 `arrDerangement` 是「数组打乱函数」，作用是**按固定规则打乱** **`_0x495`** **的元素顺序**，防止你直接查表，但是这个打乱是「确定性算法」，**输入固定 → 输出固定，没有随机因素**；

4. **函数封装**：外层的自执行函数`(function(myArr, num) { ... })()` 只是把混淆相关的变量局部化，防止外部篡改，无额外加密逻辑；

5. **补充特征**：数组里包含`Unicode转义符(\u0989)`、`不可见字符(\x85)`、`正则表达式字符串`、`业务接口地址(58同城的aris接口)`、`加密密钥`等，这些都是工具能自动还原的。

---

## ✅ 方案一：一键自动化解混淆（推荐，99%场景首选，最快最准）

### 适合人群：所有用户，无需懂原理，**复制粘贴即可得到明文代码**，耗时10秒，解决你的问题的最优解！

### 推荐3个靠谱工具（按优先级排序，均免费、无广告、无需安装、在线使用）

你的代码是标准的这种混淆格式，**这三个工具都能完美还原，没有任何适配问题**

#### 1. 最优工具：【De4js】（专治这种数组下标混淆，针对性最强）

地址：[https://lelinhtinh.github.io/de4js/](https://lelinhtinh.github.io/de4js/)

#### 操作步骤（傻瓜式，严格照做即可）：

① 复制你贴的 **全部代码**（从`var _0x495 = [`开始，到最后的`while (--nums) {`全部复制）；

② 打开上方链接，把代码粘贴到左侧的输入框中；

③ 点击页面上方的按钮：`【Unpack】` → 选择 `【Unpack Array】`（解数组混淆的核心按钮）；

④ 点击页面上方的按钮：`【Beautify】`（代码格式化，补全缩进换行）；

⑤ 右侧输出框直接生成 **100%明文的可读代码**：所有`_0x495[xx]`都被替换成对应的明文，乱序逻辑被清除，无用的混淆代码被剔除，缩进清晰，直接能看懂业务逻辑。

#### 2. 备选工具1：【JSNice】（智能还原变量名+解混淆，可读性拉满）

地址：[https://jsnice.org/](https://jsnice.org/)

- 操作：粘贴代码 → 点击`Deobfuscate` → 等待3秒，输出的代码不仅还原了字符串，还能智能把无意义的变量名（如`myArr/num`）改成语义化名称，适合需要深度阅读代码的场景。

#### 3. 备选工具2：【站长工具-JS解混淆】（国内站点，加载快，适合网络不好的情况）

地址：[https://tool.lu/js/](https://tool.lu/js/)

- 操作：粘贴代码 → 点击`解混淆` → 点击`格式化`，一步到位，效果和上面一致。

---

## ✅ 方案二：手动解混淆（理解原理，精准可控，适合复杂场景/工具失效时）

如果想自己掌握原理，或者遇到工具处理不了的嵌套混淆（比如`_0x495[_0x123[5]]`），可以用这种方法，**核心逻辑：先还原数组映射关系 → 全局替换下标 → 美化代码**，全程在浏览器控制台完成，无需额外工具。

### 完整手动还原步骤（适配你的代码结构）

#### 步骤1：提取核心字典池

把代码里的 `var _0x495 = ["161","162",..."160"];` 这一整行完整复制下来，这是你的「明文映射表」。

#### 步骤2：执行乱序函数，得到最终的【索引-明文】映射关系

你的混淆里的`arrDerangement`是**固定规则的乱序函数**，本质是 `while(--nums)` 循环交换数组元素，**无随机因子**，还原方法：

① 打开任意浏览器（Chrome/Firefox），按 `F12` → 切换到「Console(控制台)」面板；

② 粘贴你复制的 `_0x495` 数组代码，按回车执行；

③ 粘贴你的乱序函数 `var arrDerangement = function(nums) { while (--nums) { ... } }` 完整代码，按回车执行；

④ 执行乱序：在控制台输入 `arrDerangement(传入的num值)`（num是自执行函数的第二个参数，你的代码里能看到），按回车；

⑤ 此时输入 `_0x495` 并回车，控制台会打印出**打乱后的完整数组**，此时每个索引对应的明文就完全确定了，比如 `_0x495[0]` 对应的值、`_0x495[10]` 对应的值，一目了然。

#### 步骤3：全局替换下标为明文

回到你的原代码，把**所有** `_0x495[数字]` 格式的内容，全部替换成对应的明文字符串：

- 例：`_0x495[15]` → `"Anya"`

- 例：`_0x495[16]` → `"window"`

- 例：`_0x495[24]` → `"congratulations"`

#### 步骤4：清理+美化代码

① 删除无用的混淆代码：`arrDerangement`函数、外层的自执行函数壳、无意义的变量；

② 用任意格式化工具（比如[https://prettier.io/](https://prettier.io/)）美化代码，补全缩进和换行；

③ 最终得到和自动化工具还原的**完全一致的明文代码**。

---

## ✅ 关键避坑&补充知识点（必看，解决你可能遇到的问题）

### 1. 关于数组里的特殊字符还原

你的`_0x495`数组里有大量特殊字符：`\u0989`、`\u200B\x85\u180E`、`\udf06`、`\n`、`\t`等，**不用手动处理**，所有解混淆工具都会自动把这些转义符还原成「可视的原始字符」。

### 2. 这种混淆的本质是「障眼法」，无真正加密

这种混淆属于 **「代码混淆(Obfuscation)」**，不是「代码加密(Encryption)」，它只是改变了代码的书写形式，没有对代码进行加密，所以**所有这种混淆都能100%还原**，不存在解不开的情况。

### 3. 快速查表小技巧（临时看某个下标对应的明文）

如果只是想快速看某个`_0x495[xx]`是什么，不用全量解混淆：

浏览器F12→控制台→粘贴`_0x495`数组代码→回车→输入`_0x495[你要查的数字]`→回车，直接输出明文。

比如输入 `_0x495[15]` → 输出 `"Anya"`；输入 `_0x495[24]` → 输出 `"congratulations"`。

### 4. 你的代码业务内容说明（解混淆后你会看到）

这份代码是 **前端「设备指纹/风控/设备标识」相关的业务代码**，包含：

- 浏览器UA解析、设备信息采集；

- AES/Base64/HMACSHA256加密逻辑；

- 58同城的接口请求（`https://aris.58.com/web/api/gather`）；

- 本地存储(cookie/localStorage)操作；

- 反爬虫/反调试相关的检测逻辑；

- 各种异常报错的提示信息。

---

## ✅ 总结

1. 你的代码是**最基础的JS数组下标混淆**，无任何高级加密，**优先用「方案一」的De4js工具一键还原**，10秒搞定，这是最优解；

2. 工具还原后的代码是完整的、可运行的、无冗余的明文代码，所有业务逻辑清晰可见；

3. 这种混淆手段的目的只是「增加阅读难度」，而非「防止破解」，所有前端的这种混淆都能轻松还原。

按上面的方法操作，你就能完美得到解混淆后的代码了！👍
> （注：文档部分内容可能由 AI 生成）